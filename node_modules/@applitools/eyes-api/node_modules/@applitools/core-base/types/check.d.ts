import type { Target, CheckSettings, CheckResult } from './types';
import { type Logger } from '@applitools/logger';
import { type EyesRequests } from './server/requests';
type Options = {
    requests: EyesRequests;
    logger: Logger;
};
export declare function makeCheck({ requests, logger: defaultLogger }: Options): ({ target, settings, logger, }: {
    target: Target;
    settings?: CheckSettings<import("@applitools/utils").Region> | undefined;
    logger?: Logger | undefined;
}) => Promise<CheckResult[]>;
export {};
