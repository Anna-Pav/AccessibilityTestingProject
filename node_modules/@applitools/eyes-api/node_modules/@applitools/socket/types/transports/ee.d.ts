/// <reference types="node" />
import { type Transport } from '../transport';
import { type EventEmitter } from 'events';
export type Options = {
    events: {
        message: string;
        emit: string;
    };
};
export declare function makeTransport(options: Options): Transport<EventEmitter>;
export declare const transport: Transport<EventEmitter>;
export default transport;
