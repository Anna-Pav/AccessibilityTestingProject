import type { ECClient, ECClientSettings } from './types';
import { type Logger } from '@applitools/logger';
export declare function makeServer({ settings, logger }: {
    settings: ECClientSettings;
    logger?: Logger;
}): Promise<ECClient>;
