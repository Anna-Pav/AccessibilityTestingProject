/// <reference types="node" />
import { type IncomingMessage, type ServerResponse } from 'http';
import { type Logger } from '@applitools/logger';
import { type Proxy, type Retry, type Options } from '@applitools/req';
export type ReqProxyConfig = {
    targetUrl: string;
    proxy?: Proxy;
    retry?: Retry | Retry[];
    logger?: Logger;
};
export type ReqProxyOptions = Options & {
    io: {
        request: IncomingMessage;
        response: ServerResponse;
        handle?: boolean;
    };
    logger?: Logger;
};
export declare function makeReqProxy(config: ReqProxyConfig): import("@applitools/req").Req<ReqProxyOptions>;
